# BREAKPOINT — Dependencies

# Core agent
openai>=1.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Terminal UI
rich>=13.7.0

# HTTP server (Person 4 integration)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Optional: Playwright (Person 2 integration)
# playwright>=1.44.0

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
