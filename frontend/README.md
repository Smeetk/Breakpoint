# Demo Store frontend

The target application frontend is served by the root `index.html` so the
existing `python server.py` command continues to work at `http://localhost:8000/`.

This folder is the frontend ownership area for Person 3. The frontend entry
point is available at `../index.html`.
